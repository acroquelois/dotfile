- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc

- create:
    - ~/downloads
    - ~/perso
    - ~/pro
    - ~/tmp
    - ~/data

- shell:
    - [git submodule update --init --recursive, Installing submodules]