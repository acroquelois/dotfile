- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- create:
      - ~/downloads
      - ~/perso
      - ~/pro
      - ~/tmp
      - ~/data
      -
- link:
    ~/.zshrc: zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]