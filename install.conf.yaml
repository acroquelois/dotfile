- shell:
    - command: apt install -t zsh
      description: Installing base software
      stdin: true
      stdout: true
    - command: apt install -y yarn nodejs npm nvm vim
      description: Installing codding tools
      stdin: true
      stdout: true